---
# Standalone playbook to apply node hardening only.
# Run with --check --diff first to preview changes:
#   ansible-playbook playbooks/node-hardening.yml --check --diff
- name: Harden all cluster nodes
  hosts: k3s_cluster
  gather_facts: true
  become: true
  roles:
    - node-hardening
