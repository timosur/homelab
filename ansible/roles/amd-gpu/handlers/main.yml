---
- name: Reboot for DKMS module
  ansible.builtin.reboot:
    reboot_timeout: 300
    msg: "Rebooting to load new amdgpu DKMS module"

- name: Reload systemd and restart ollama
  ansible.builtin.systemd:
    name: ollama
    state: restarted
    daemon_reload: true
  become: true
