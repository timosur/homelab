apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: main-gateway
  namespace: cert-manager
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  gatewayClassName: cilium
  listeners:
    - name: argo-acme-http
      port: 80
      protocol: HTTP
      hostname: "argo.timosur.com"
      allowedRoutes:
        namespaces:
          from: All
    # - name: argo-https
    #   port: 443
    #   protocol: HTTPS
    #   hostname: "argo.timosur.com"
    #   allowedRoutes:
    #     namespaces:
    #       from: All
    #   tls:
    #     mode: Terminate
    #     certificateRefs:
    #       - name: argo-timosur-com
    - name: mealie-acme-http
      port: 80
      protocol: HTTP
      hostname: "mealie.timosur.com"
      allowedRoutes:
        namespaces:
          from: All
    # - name: mealie-https
    #   port: 443
    #   protocol: HTTPS
    #   hostname: "mealie.timosur.com"
    #   allowedRoutes:
    #     namespaces:
    #       from: All
    #   tls:
    #     mode: Terminate
    #     certificateRefs:
    #       - name: mealie-timosur-com
