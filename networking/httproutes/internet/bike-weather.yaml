apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: bike-weather
  namespace: bike-weather
spec:
  parentRefs:
    - name: envoy-gateway-internet
      namespace: envoy-gateway-internet-system
      sectionName: bike-weather-https
  hostnames:
    - "bike-weather.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: bike-weather-nginx
          port: 80
