apiVersion: v1
kind: Service
metadata:
  name: seafile
  namespace: seafile
spec:
  selector:
    app: seafile
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
    - name: webdav
      protocol: TCP
      port: 8080
      targetPort: 8080
