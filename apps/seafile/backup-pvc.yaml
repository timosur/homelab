apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seafile-mysql-backup-storage
  namespace: seafile
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  storageClassName: storage-box-smb