apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: coding-tools-mcp
  namespace: agentgateway-system
spec:
  mcp:
    targets:
      - name: coding-tools-mcp
        static:
          host: coding-tools-mcp.mcp.svc.cluster.local
          port: 80
          path: /mcp
