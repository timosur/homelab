# ExternalName service routing to wol-proxy ollama
apiVersion: v1
kind: Service
metadata:
  name: ollama
  namespace: agentgateway-system
spec:
  type: ExternalName
  externalName: ollama-service.wol-proxy.svc.cluster.local
  ports:
    - port: 11434
      protocol: TCP
