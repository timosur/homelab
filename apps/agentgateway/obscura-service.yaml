apiVersion: v1
kind: Service
metadata:
  name: obscura
  namespace: agentgateway-system
spec:
  selector:
    app.kubernetes.io/name: obscura
  ports:
    - name: mcp
      port: 8080
      targetPort: mcp
      appProtocol: agentgateway.dev/mcp
