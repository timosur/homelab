apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: kagi-mcp
  namespace: agentgateway-system
spec:
  mcp:
    targets:
      - name: kagi
        static:
          host: mcp.kagi.com
          port: 443
          path: /mcp
          protocol: StreamableHTTP
          policies:
            tls:
              sni: mcp.kagi.com
            auth:
              credentials:
                - secretRef:
                    name: agentgateway-mcp-api-keys
                    key: kagi-api-key
                  location:
                    header:
                      name: Authorization
                      prefix: "Bearer "
