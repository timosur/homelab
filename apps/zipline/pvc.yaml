apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zipline-data
  namespace: zipline
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 25Gi
  storageClassName: hcloud-volumes
