apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources: []
# Add Agent CRDs, ExternalSecrets, and network policies here when ready.
# Example:
#   - agent.yaml
#   - external-secret.yaml
#   - cilium-network-policy.yaml
