apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: affine-redis-data
  namespace: affine
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: homelab-smb
  resources:
    requests:
      storage: 1Gi
