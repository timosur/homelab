apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: affine-storage
  namespace: affine
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: homelab-smb
  resources:
    requests:
      storage: 10Gi
