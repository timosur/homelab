apiVersion: batch/v1
kind: Job
metadata:
  name: affine-migration
  namespace: affine
spec:
  backoffLimit: 6
  template:
    spec:
      restartPolicy: OnFailure
      initContainers:
        - name: copy-config
          image: docker.io/library/busybox:1.37
          command: ["sh", "-c", "mkdir -p /config && test -f /config/config.json || cp /defaults/config.json /config/config.json"]
          volumeMounts:
            - name: config-data
              mountPath: /config
            - name: config-defaults
              mountPath: /defaults
              readOnly: true
      containers:
        - name: migration
          image: ghcr.io/toeverything/affine:stable
          command: ["sh", "-c", "node ./scripts/self-host-predeploy.js"]
          env:
            - name: DEPLOYMENT_TYPE
              value: selfhosted
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: affine-db-connection
                  key: uri
            - name: REDIS_SERVER_HOST
              value: affine-redis
            - name: REDIS_SERVER_PORT
              value: "6379"
            - name: AFFINE_INDEXER_ENABLED
              value: "false"
          volumeMounts:
            - name: storage
              mountPath: /root/.affine/storage
            - name: config-data
              mountPath: /root/.affine/config
          resources:
            requests:
              memory: 512Mi
              cpu: 200m
            limits:
              memory: 2Gi
              cpu: 1
      volumes:
        - name: storage
          persistentVolumeClaim:
            claimName: affine-storage
        - name: config-data
          persistentVolumeClaim:
            claimName: affine-storage
        - name: config-defaults
          configMap:
            name: affine-config
