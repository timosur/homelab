apiVersion: apps/v1
kind: Deployment
metadata:
  name: bike-weather-preview-frontend
  namespace: bike-weather-preview
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: bike-weather-preview-frontend
  template:
    metadata:
      labels:
        app: bike-weather-preview-frontend
    spec:
      containers:
        - name: frontend
          image: ghcr.io/timosur/bike-weather/frontend:0.0.1-preview.2
          ports:
            - containerPort: 80
          env:
            - name: VITE_AUTHENTIK_URL
              value: "https://auth.bike-weather.com/application/o/bike-weather-preview/"
            - name: VITE_AUTHENTIK_CLIENT_ID
              value: "bike-weather-preview"
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 60
          readinessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "250m"
