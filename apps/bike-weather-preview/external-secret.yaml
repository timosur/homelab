apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: bike-weather-preview-secrets
  namespace: bike-weather-preview
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: azure-keyvault-store
    kind: ClusterSecretStore
  target:
    name: bike-weather-preview-secrets
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        DATABASE_URL: "postgresql+asyncpg://app:{{ .dbPassword }}@bike-weather-preview-postgres-rw:5432/app"
        AUTHENTIK_API_TOKEN: "{{ .authentikApiToken }}"
        TURNSTILE_SECRET_KEY: "{{ .turnstileSecretKey }}"
  data:
    - secretKey: dbPassword
      remoteRef:
        key: bike-weather-preview-postgres-password
    - secretKey: authentikApiToken
      remoteRef:
        key: bike-weather-preview-authentik-api-token
    - secretKey: turnstileSecretKey
      remoteRef:
        key: bike-weather-preview-turnstile-secret-key
