apiVersion: v1
kind: Service
metadata:
  name: bike-weather-preview-agent
  namespace: bike-weather-preview
spec:
  selector:
    app: bike-weather-preview-agent
  ports:
    - name: http
      port: 8001
      targetPort: 8001
  type: ClusterIP
