apiVersion: v1
kind: Service
metadata:
  name: bike-weather-preview-backend
  namespace: bike-weather-preview
spec:
  selector:
    app: bike-weather-preview-backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
