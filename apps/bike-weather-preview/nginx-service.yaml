apiVersion: v1
kind: Service
metadata:
  name: bike-weather-preview-nginx
  namespace: bike-weather-preview
spec:
  selector:
    app: bike-weather-preview-nginx
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
