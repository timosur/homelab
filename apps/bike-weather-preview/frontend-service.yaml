apiVersion: v1
kind: Service
metadata:
  name: bike-weather-preview-frontend
  namespace: bike-weather-preview
spec:
  selector:
    app: bike-weather-preview-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
