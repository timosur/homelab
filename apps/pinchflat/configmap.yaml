apiVersion: v1
kind: ConfigMap
metadata:
  name: pinchflat-config
  namespace: pinchflat
data:
  TZ: "Europe/Berlin"
  # Database optimization for SQLite
  ECTO_POOL_SIZE: "10"
  ECTO_QUEUE_TARGET: "5000"
  ECTO_QUEUE_INTERVAL: "1000"
