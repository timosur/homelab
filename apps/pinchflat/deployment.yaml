apiVersion: apps/v1
kind: Deployment
metadata:
  name: pinchflat
  namespace: pinchflat
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: pinchflat
  template:
    metadata:
      labels:
        app: pinchflat
    spec:
      securityContext:
        fsGroup: 1000
      initContainers:
        - name: setup-cookies
          image: busybox:latest
          command: ["sh", "-c"]
          args:
            - |
              mkdir -p /config/extras
              if [ -f /tmp/cookies/cookies.txt ]; then
                cp /tmp/cookies/cookies.txt /config/extras/cookies.txt
              fi
          volumeMounts:
            - name: pinchflat-config
              mountPath: /config
            - name: pinchflat-cookies-secret
              mountPath: /tmp/cookies
              readOnly: true
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            runAsNonRoot: true
        - name: wireguard
          image: linuxserver/wireguard:latest
          env:
            - name: TZ
              value: "Europe/Berlin"
          volumeMounts:
            - name: pinchflat-wireguard-config
              mountPath: /etc/wireguard/
              readOnly: true
          securityContext:
            privileged: true
            capabilities:
              add:
                - NET_ADMIN
          restartPolicy: Always
          resources:
            limits:
              memory: "128Mi"
              cpu: "100m"
            requests:
              memory: "64Mi"
              cpu: "50m"
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/arch
                    operator: In
                    values:
                      - amd64
                      - x86_64
      containers:
        - name: pinchflat
          image: ghcr.io/kieraneglin/pinchflat:latest
          ports:
            - containerPort: 8945
          envFrom:
            - configMapRef:
                name: pinchflat-config
            - secretRef:
                name: pinchflat-auth-credentials
          volumeMounts:
            - name: pinchflat-config
              mountPath: /config
            - name: pinchflat-downloads
              mountPath: /downloads
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            runAsNonRoot: true
          resources:
            limits:
              memory: "1000Mi"
              cpu: "1000m"
            requests:
              memory: "256Mi"
              cpu: "100m"
          livenessProbe:
            tcpSocket:
              port: 8945
            initialDelaySeconds: 60
            periodSeconds: 30
            failureThreshold: 3
            timeoutSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 8945
            initialDelaySeconds: 30
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 3
      volumes:
        - name: pinchflat-config
          persistentVolumeClaim:
            claimName: pinchflat-config
        - name: pinchflat-downloads
          persistentVolumeClaim:
            claimName: shared-downloads
        - name: pinchflat-cookies-secret
          secret:
            secretName: pinchflat-cookies
        - name: pinchflat-wireguard-config
          secret:
            secretName: pinchflat-wireguard-config
