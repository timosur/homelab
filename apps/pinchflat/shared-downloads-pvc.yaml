apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-downloads
  namespace: pinchflat
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: storage-box-smb
