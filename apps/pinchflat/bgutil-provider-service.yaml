apiVersion: v1
kind: Service
metadata:
  name: bgutil-provider
  namespace: pinchflat
spec:
  selector:
    app: bgutil-provider
  ports:
    - name: http
      port: 4416
      targetPort: 4416
      protocol: TCP
  type: ClusterIP
