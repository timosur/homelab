apiVersion: v1
kind: Service
metadata:
  name: vinyl-audio-analyzer
  namespace: vinyl-manager
spec:
  selector:
    app: vinyl-audio-analyzer
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000