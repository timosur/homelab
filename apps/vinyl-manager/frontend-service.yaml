apiVersion: v1
kind: Service
metadata:
  name: vinyl-frontend
  namespace: vinyl-manager
spec:
  selector:
    app: vinyl-frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
