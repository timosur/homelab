apiVersion: v1
kind: Service
metadata:
  name: bike-weather-nginx
  namespace: bike-weather
spec:
  selector:
    app: bike-weather-nginx
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
