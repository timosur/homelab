apiVersion: v1
kind: Service
metadata:
  name: bike-weather-backend
  namespace: bike-weather
spec:
  selector:
    app: bike-weather-backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
