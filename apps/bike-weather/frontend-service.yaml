apiVersion: v1
kind: Service
metadata:
  name: bike-weather-frontend
  namespace: bike-weather
spec:
  selector:
    app: bike-weather-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
