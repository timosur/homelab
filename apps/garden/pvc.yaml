apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: garden-backend-db
  namespace: garden
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: hcloud-volumes
