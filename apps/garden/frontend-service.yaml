apiVersion: v1
kind: Service
metadata:
  name: garden-frontend
  namespace: garden
spec:
  selector:
    app: garden-frontend
  ports:
  - name: http
    port: 80
    targetPort: 80
  type: ClusterIP
