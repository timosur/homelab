apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: garden-backup-storage
  namespace: garden
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: storage-box-smb
