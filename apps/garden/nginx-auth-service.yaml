apiVersion: v1
kind: Service
metadata:
  name: garden-nginx-auth
  namespace: garden
spec:
  selector:
    app: garden-nginx-auth
  ports:
  - name: http
    port: 80
    targetPort: 80
  type: ClusterIP
