apiVersion: v1
kind: Service
metadata:
  name: garden-backend
  namespace: garden
spec:
  selector:
    app: garden-backend
  ports:
  - name: http
    port: 8000
    targetPort: 8000
  type: ClusterIP
