apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: garden-postgres-credentials
  namespace: garden
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: azure-keyvault-store
    kind: ClusterSecretStore
  target:
    name: garden-postgres-credentials
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
  data:
    - secretKey: username
      remoteRef:
        key: garden-postgres-username
    - secretKey: password
      remoteRef:
        key: garden-postgres-password
