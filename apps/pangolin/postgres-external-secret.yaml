apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: pangolin-postgres-credentials
  namespace: pangolin
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: azure-keyvault-store
    kind: ClusterSecretStore
  target:
    name: pangolin-postgres-credentials
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
  data:
    - secretKey: username
      remoteRef:
        key: pangolin-postgres-username
    - secretKey: password
      remoteRef:
        key: pangolin-postgres-password