apiVersion: v1
kind: Service
metadata:
  name: coding-tools-mcp
  namespace: mcp
  labels:
    app: coding-tools-mcp
spec:
  selector:
    app: coding-tools-mcp
  ports:
    - port: 80
      targetPort: 8080
      appProtocol: agentgateway.dev/mcp
