apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bike-weather-auth-media
  namespace: bike-weather-auth
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: hcloud-volumes
  resources:
    requests:
      storage: 5Gi
