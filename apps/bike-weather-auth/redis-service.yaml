apiVersion: v1
kind: Service
metadata:
  name: bike-weather-auth-redis
  namespace: bike-weather-auth
spec:
  selector:
    app: bike-weather-auth-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
  type: ClusterIP
