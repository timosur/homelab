apiVersion: v1
kind: Service
metadata:
  name: mealie-mcp-server
  namespace: mealie
spec:
  selector:
    app: mealie-mcp-server
  ports:
  - name: http
    port: 8000
    targetPort: 8000
  type: ClusterIP
