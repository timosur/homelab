apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kagent-postgres-credentials
  namespace: agents
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: azure-keyvault-store
    kind: ClusterSecretStore
  target:
    name: kagent-postgres-credentials
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
  data:
    - secretKey: username
      remoteRef:
        key: kagent-postgres-username
    - secretKey: password
      remoteRef:
        key: kagent-postgres-password
